{
  "name": "ynab-mcp-worker",
  "version": "1.0.0",
  "description": "Remote MCP server for YNAB (You Need A Budget) on Cloudflare Workers, deployable as a claude.ai custom connector.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.134.0"
  }
}
